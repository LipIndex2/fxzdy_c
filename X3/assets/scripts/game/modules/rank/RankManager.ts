import { _decorator, } from 'cc';
import BaseSingleton from "db://assets/scripts/core/base/BaseSingleton";

const {ccclass, property} = _decorator;

export class RankManager extends BaseSingleton {

    onInit(): void {
        // 绑定脚本给组件
        // G.FGUIManager.bindScript("ui://backpack/iconItem", BackpackItemView);
        
        
    }

}
